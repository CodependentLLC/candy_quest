{
  "name": "candy-quest-sugar-rush",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node test/smoke.mjs && node test/server.mjs",
    "test:e2e": "playwright test",
    "validate:assets": "node tools/validate-assets.mjs",
    "test:release": "npm run validate:assets && npm test && npm run test:e2e"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
